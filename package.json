{
  "name": "mj-forge",
  "version": "0.3.4",
  "description": "AI-native SQL Server IDE for macOS and Windows",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "build:stream": "turbo run build --log-order=stream",
    "dev": "npm run build && concurrently -k \"npm run dev:renderer\" \"npm run dev:main\"",
    "dev:renderer": "npm run start --workspace=@mj-forge/renderer",
    "dev:main": "wait-on http://localhost:4200 && unset ELECTRON_RUN_AS_NODE && NODE_ENV=development npm run start --workspace=@mj-forge/main",
    "start": "turbo run start",
    "watch": "turbo run watch",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,json,md,scss,css,html}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md,scss,css,html}\"",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "deps:check": "syncpack list-mismatches",
    "deps:fix": "syncpack fix-mismatches",
    "package": "npm run build && node scripts/prepare-package.js && electron-builder --config electron-builder.yml",
    "package:mac": "npm run build && node scripts/prepare-package.js && electron-builder --mac --config electron-builder.yml",
    "package:dir": "npm run build && node scripts/prepare-package.js && electron-builder --dir --config electron-builder.yml",
    "prepare": "husky"
  },
  "main": "packages/main/dist/index.js",
  "dependencies": {
    "@memberjunction/sqlglot-ts": "^5.23.0",
    "dockerode": "^4.0.2",
    "electron-store": "^8.2.0",
    "keytar": "^7.9.0",
    "mssql": "^11.0.1",
    "pg": "^8.20.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "@playwright/test": "^1.58.2",
    "@types/jest": "^29.5.14",
    "@types/pg": "^8.20.0",
    "@typescript-eslint/eslint-plugin": "^7.12.0",
    "@typescript-eslint/parser": "^7.12.0",
    "concurrently": "^9.2.1",
    "electron": "^41.0.3",
    "electron-builder": "26.4.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "lint-staged": "^16.2.7",
    "prettier": "^3.3.1",
    "syncpack": "^12.3.2",
    "ts-jest": "^29.4.6",
    "turbo": "^2.3.3",
    "typescript": "~5.4.5",
    "wait-on": "^9.0.3"
  },
  "overrides": {
    "@types/node": "^20.14.2",
    "typescript": "~5.4.5"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,scss,css,html}": [
      "prettier --write"
    ]
  }
}
